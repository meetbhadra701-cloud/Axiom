# Yosys synthesis self-check for the active module (CLAUDE.md §3, objective (c)).
# Run: yosys -s synth/check.ys

read_verilog -sv rtl/barrel_shift.v
hierarchy -check -top barrel_shift
proc
opt
synth -top barrel_shift
check -assert
stat
