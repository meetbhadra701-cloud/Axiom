# Yosys synthesis self-check for the active module (CLAUDE.md §3, objective (c)).
# Run: yosys -s synth/check.ys

read_verilog -sv rtl/sine_lut.v
hierarchy -check -top sine_lut
proc
opt
synth -top sine_lut
check -assert
stat
