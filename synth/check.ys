# Yosys synthesis self-check for the active module (CLAUDE.md §3, objective (c)).
# Run: yosys -s synth/check.ys

read_verilog -sv rtl/debounce.v
hierarchy -check -top debounce
proc
opt
synth -top debounce
check -assert
stat
