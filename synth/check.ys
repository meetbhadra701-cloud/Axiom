# Yosys synthesis self-check for the active module (CLAUDE.md §3, objective (c)).
# Run: yosys -s synth/check.ys

read_verilog -sv rtl/edge_det.v
hierarchy -check -top edge_det
proc
opt
synth -top edge_det
check -assert
stat
