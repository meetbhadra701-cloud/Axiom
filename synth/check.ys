# Yosys synthesis self-check for the active module (CLAUDE.md §3, objective (c)).
# Run: yosys -s synth/check.ys

read_verilog -sv rtl/delta_sigma.v
hierarchy -check -top delta_sigma
proc
opt
synth -top delta_sigma
check -assert
stat
