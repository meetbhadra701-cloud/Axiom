# Yosys synthesis self-check for the active module (CLAUDE.md §3, objective (c)).
# Run: yosys -s synth/check.ys

read_verilog -sv rtl/iir_biquad.v
hierarchy -check -top iir_biquad
proc
opt
synth -top iir_biquad
check -assert
stat
